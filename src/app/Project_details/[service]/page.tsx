import ProjectForm from "@/components/Project-Details-Form";

// Define the Pages component
const Pages = () => {
  return (
    <div>
      <ProjectForm /> {/* Render the ProjectForm component */}
    </div>
  );
};

export default Pages; // Export the Pages component
